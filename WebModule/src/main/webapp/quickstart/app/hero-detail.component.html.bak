							
							
							
							
							
							<div *ngIf="heroe">
	  						<h2>{{heroe.name}} details!</h2>
	  						<div>
	  							<label>id: </label>
	  							{{heroe.id}}
	  						</div>
  						
	  						<div>
	  							<label>name: </label>
	  							 <input [(ngModel)]="heroe.name" placeholder="name">
	  						</div>
  						</div>
  						<button (click)="goBack()">Back</button>